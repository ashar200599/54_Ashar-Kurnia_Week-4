lab = res_df_filter$Gene.Names,
x = 'log2FoldChange',
y = 'padj',
selectLab = top10_gene_bind$Gene.Names,
title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
pCutoff = 0.05,
FCcutoff = 1
)
# Convert DESeq2 result into dataframe
res_df <- as.data.frame(res)
#Filter pvalue and Log2FoldChange
res_df_filter <- res_df[(res_df$padj < 0.05) & (abs(res_df$log2FoldChange) > 2.5),]
res_df_filter <- na.omit(res_df)
#Ordered padj
top10_gene_ordered <- res_df_filter[order(res_df_filter$padj, res_df_filter$log2FoldChange), ]
# Top10 gene base on padj and fold2 change
top10_gene <- head(top10_gene_ordered, 10)
top10_gene_df <- as.data.frame(top10_gene)
#Enhanced volcano
EnhancedVolcano(
res_df_filter,
lab = res_df_filter$Gene.Names,
x = 'log2FoldChange',
y = 'padj',
selectLab = top10_gene_bind$Gene.Names,
title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
pCutoff = 0.05,
FCcutoff = 1
)
#Set upregulated and Downregulated gene
up <- top10_gene_df[top10_gene_df$log2FoldChange > 2.5, ]
down <- top10_gene_df[top10_gene_df$log2FoldChange < 0.5, ]
#Merge upregulated and downregulated gene
top10_gene_bind <- rbind(up, down)
#Enhanced volcano
EnhancedVolcano(
res_df_filter,
lab = res_df_filter$Gene.Names,
x = 'log2FoldChange',
y = 'padj',
selectLab = top10_gene_bind$Gene.Names,
title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
pCutoff = 0.05,
FCcutoff = 1
)
View(res_df_filter)
#Add gene ID column
res_df_filter$Gene.ID <- rownames(res_df_filter)
top10_gene_df$Gene.ID <- rownames(top10_gene_df)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_genes_df), keytype = "ENSEMBL", column = "SYMBOL")
View(top10_gene_df)
#Add gene ID column
res_df_filter$Gene.ID <- rownames(res_df_filter)
top10_gene_df$Gene.ID <- rownames(top10_gene_df)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_genes_df), keytype = "ENSEMBL", column = "SYMBOL")
View(res_df_filter)
#Filter pvalue and Log2FoldChange
res_df_filter <- res_df[(res_df$padj < 0.05) & (abs(res_df$log2FoldChange) > 2.5),]
res_df_filter <- na.omit(res_df)
#Ordered padj
top10_gene_ordered <- res_df_filter[order(res_df_filter$padj, res_df_filter$log2FoldChange), ]
# Top10 gene base on padj and fold2 change
top10_gene <- head(top10_gene_ordered, 10)
top10_gene_df <- as.data.frame(top10_gene)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_genes_df), keytype = "ENSEMBL", column = "SYMBOL")
View(top10_gene_df)
#Enhanced volcano
EnhancedVolcano(
res_df_filter,
lab = res_df_filter$Gene.Names,
x = 'log2FoldChange',
y = 'padj',
selectLab = top10_gene_bind$Gene.Names,
title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
pCutoff = 0.05,
FCcutoff = 1
)
#Enhanced volcano
volcano <-  EnhancedVolcano(
res_df_filter,
lab = res_df_filter$Gene.Names,
x = 'log2FoldChange',
y = 'padj',
selectLab = top10_gene_bind$Gene.Names,
title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
pCutoff = 0.05,
FCcutoff = 1
)
print(volcano)
mat <- counts(dds,normalized = T)[rownames(top10_genes_df),]
mat <- counts(dds,normalized = T)[rownames(top10_genes_df),]
mat <- counts(dds,normalized = T)[rownames(top10_gene_df),]
h <- Heatmap(mat, cluster_rows=T,column_labels = colnames(mat),
name="Z-score", row_labels=top10_gene_df[rownames(mat),]$Gene.Names)
View(top10_gene)
mat <- counts(dds,normalized = T)[rownames(top10_gene_df),]
h <- Heatmap(mat, cluster_rows=T,column_labels = colnames(mat),
name="Z-score", row_labels=top10_gene_df[rownames(mat),]$Gene.Names)
mat <- counts(dds,normalized = T)[rownames(top10_gene_df),]
h <- Heatmap(mat, cluster_rows=T,column_labels = colnames(mat),
name="Z-score", row_labels=top10_gene_df[rownames(mat),]$Gene.Names)
dim(mat)
dim(top10_gene_df)
#Add gene ID column
res_df_filter$Gene.ID <- rownames(res_df_filter)
top10_gene_df$Gene.ID <- rownames(top10_gene_df)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_genes_df), keytype = "ENSEMBL", column = "SYMBOL")
View(top10_gene_df)
View(top10_gene_bind)
View(top10_gene)
View(top10_gene_bind)
View(top10_gene_df)
View(res_df_filter)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_genes_df), keytype = "ENSEMBL", column = "SYMBOL")
#Load library
library(DESeq2)
library(EnhancedVolcano)
library(ggplot2)
library(ComplexHeatmap)
library(org.Hs.eg.db)
library(AnnotationDbi)
library(fontLiberation)
library(clusterProfiler)
library(tidyverse)
# Download raw counts from EBI
counts = read.delim("https://www.ebi.ac.uk/gxa/experiments-content/E-MTAB-9479/resources/DifferentialSecondaryDataFiles.RnaSeq/raw-counts")
head(counts)
# Download metadata
metadata = read.delim("https://www.ebi.ac.uk/gxa/experiments-content/E-MTAB-9479/resources/ExperimentDesignFile.RnaSeq/experiment-design")
head(metadata)
# Counts data wrangling
# DESeq expects the counts to have gene IDs as row names
counts$Gene.ID <- as.character(counts$Gene.ID)
rownames(counts) <- counts$Gene.ID
head(counts)
# Separate gene ID with gene counts
genes = counts[, c("Gene.ID", "Gene.Name"),drop=FALSE]
genes = na.omit(genes) #optional
counts = counts[, -c(1, 2)]
head(counts)
#Metadata Wrangling
# DESeq expects the metadata matrix to have sample IDs in the rownames
metadata$Run <- as.character(metadata$Run)
rownames(metadata) = metadata$Run
head(metadata)
# Only keep columns of interest
metadata = metadata[, c("Sample.Characteristic.stimulus."), drop=FALSE] #Drop are used to make data frame with one column
# Look at metadata to see how the variables change with respect to each other
metadata
# Rename column
colnames(metadata) = "treatment"
# Turn stimulus/treatment into a factor
metadata$treatment = factor(metadata$treatment, levels =c("BMP2", "PBS"))
dds <- DESeqDataSetFromMatrix(countData=counts, colData=metadata, design=~treatment)
# Ignore genes with low counts
dds <- dds[rowSums(counts(dds)) > 10, ]
# Run DESeq
dds <- DESeq(dds)
# Compare expression
res <- results(dds, contrast=c("treatment", "BMP2", "PBS"), alpha=0.05)
na.omit(res)
# Convert DESeq2 result into dataframe
res_df <- as.data.frame(res)
#Filter pvalue and Log2FoldChange
res_df_filter <- res_df[(res_df$padj < 0.05) & (abs(res_df$log2FoldChange) > 2.5),]
res_df_filter <- na.omit(res_df)
#Ordered padj
top10_gene_ordered <- res_df_filter[order(res_df_filter$padj, res_df_filter$log2FoldChange), ]
# Top10 gene base on padj and fold2 change
top10_gene <- head(top10_gene_ordered, 10)
top10_gene_df <- as.data.frame(top10_gene)
View(top10_gene)
View(top10_gene_df)
#Add gene ID column
res_df_filter$Gene.ID <- rownames(res_df_filter)
top10_gene_df$Gene.ID <- rownames(top10_gene_df)
View(res_df_filter)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_genes_df), keytype = "ENSEMBL", column = "SYMBOL")
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_gene_df), keytype = "ENSEMBL", column = "SYMBOL")
View(top10_gene_df)
# Normalized count from DESeq2 result
norm_counts <- counts(dds, normalized = TRUE)
#Set upregulated and Downregulated gene
up <- top10_gene_df[top10_gene_df$log2FoldChange > 2.5, ]
down <- top10_gene_df[top10_gene_df$log2FoldChange < 0.5, ]
#Merge upregulated and downregulated gene
top10_gene_bind <- rbind(up, down)
#Enhanced volcano
volcano <-  EnhancedVolcano(
res_df_filter,
lab = res_df_filter$Gene.Names,
x = 'log2FoldChange',
y = 'padj',
selectLab = top10_gene_bind$Gene.Names,
title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
pCutoff = 0.05,
FCcutoff = 1
)
print(volcano)
mat <- counts(dds,normalized = T)[rownames(top10_gene_df),]
h <- Heatmap(mat, cluster_rows=T,column_labels = colnames(mat),
name="Z-score", row_labels=top10_gene_df[rownames(mat),]$Gene.Names)
mat <- counts(dds,normalized = T)[rownames(top10_gene_df),]
h <- Heatmap(mat,
cluster_rows=T,
column_labels = colnames(mat),
title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
name="Z-score",
row_labels=top10_gene_df[rownames(mat),]$Gene.Names)
mat <- counts(dds,normalized = T)[rownames(top10_gene_df),]
h <- Heatmap(mat,
cluster_rows=T,
column_labels = colnames(mat),
column_title = "Top 10 Gene Expression of BMP signaling in Ovarian Cancer Gene",
name="Z-score",
row_labels=top10_gene_df[rownames(mat),]$Gene.Names)
print(h)
#Take data to test
genes_to_test <- rownames(top10_gene_df)
#GO Analysis
GO_results <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "BP")
#GO Analysis
GO_results <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "BP", "MF", "CC")
#GO Analysis
GO_results <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "ALL")
as.data.frame(GO_results)
fit <- plot(barplot(GO_results, showCategory = 20))
#GO Analysis
GO_results <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "ALL", pvalueCutoff = 0.05, pool = FALSE)
as.data.frame(GO_results)
#GO barplot
fit <- plot(barplot(GO_results, showCategory = 20))
GO_results_MF <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "MF", pvalueCutoff = 0.05)
fit <- plot(barplot(GO_results_MF, showCategory = 20))
GO_results_CC <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "CC", pvalueCutoff = 0.05)
#GO barplot
fit_BP <- plot(barplot(GO_results_BP, showCategory = 20))
#GO Analysis
GO_results_BP <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "BP", pvalueCutoff = 0.05)
GO_results_MF <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "MF", pvalueCutoff = 0.05)
GO_results_CC <- enrichGO(gene = genes_to_test, OrgDb = "org.Hs.eg.db", keyType = "ENSEMBL", ont = "CC", pvalueCutoff = 0.05)
as.data.frame(GO_results)
#GO barplot
fit_BP <- plot(barplot(GO_results_BP, showCategory = 20))
fit_MF <- plot(barplot(GO_results_MF, showCategory = 20))
fit_CC <- plot(barplot(GO_results_CC, showCategory = 20))
getwd()
#Export GO barplot
png("GO barplot_BP.png", res = 250, width=1400, height = 2000)
print(fit_BP)
png("GO barplot_MF.png", res = 250, width=1400, height = 2000)
print(fit_MF)
png("GO barplot_CC.png", res = 250, width=1400, height = 2000)
print(fit_Cc)
png("GO barplot_CC.png", res = 250, width=1400, height = 2000)
print(fit_CC)
#Export GO barplot
png("GO barplot_BP.png", res = 250, width=1400, height = 2000)
print(fit_BP)
png("GO barplot_MF.png", res = 250, width=1400, height = 2000)
print(fit_MF)
png("GO barplot_CC.png", res = 250, width=1400, height = 2000)
print(fit_CC)
dev.off()
fit
#Export GO barplot
png("GO barplot_BP.png", res = 250, width=1400, height = 2000)
print(fit_BP)
png("GO barplot_MF.png", res = 250, width=1400, height = 2000)
print(fit_MF)
png("GO barplot_CC.png", res = 250, width=1400, height = 2000)
print(fit_CC)
dev.off()
fit
#Export GO barplot
png("GO barplot_BP.png", res = 250, width=1400, height = 2000)
print(fit_BP)
dev.off()
png("GO barplot_MF.png", res = 250, width=1400, height = 2000)
print(fit_MF)
dev.off()
png("GO barplot_CC.png", res = 250, width=1400, height = 2000)
print(fit_CC)
dev.off()
#Save R environemnt(optional)
save.image(file = "54_Ashar Kurnia_Capstone_Project.Rdata")
setwd("F:/Tugas BRSP OmicsLite/Week 4/script")
#Save R environemnt(optional)
save.image(file = "54_Ashar Kurnia_Capstone_Project.RData")
#Load library
library(DESeq2)
library(EnhancedVolcano)
library(ggplot2)
library(ComplexHeatmap)
library(org.Hs.eg.db)
library(AnnotationDbi)
library(fontLiberation)
library(clusterProfiler)
library(tidyverse)
# Download raw counts from EBI
counts = read.delim("https://www.ebi.ac.uk/gxa/experiments-content/E-MTAB-9479/resources/DifferentialSecondaryDataFiles.RnaSeq/raw-counts")
head(counts)
# Download metadata
metadata = read.delim("https://www.ebi.ac.uk/gxa/experiments-content/E-MTAB-9479/resources/ExperimentDesignFile.RnaSeq/experiment-design")
head(metadata)
# Counts data wrangling
# DESeq expects the counts to have gene IDs as row names
counts$Gene.ID <- as.character(counts$Gene.ID)
rownames(counts) <- counts$Gene.ID
head(counts)
# Separate gene ID with gene counts
genes = counts[, c("Gene.ID", "Gene.Name"),drop=FALSE]
genes = na.omit(genes) #optional
counts = counts[, -c(1, 2)]
head(counts)
#Metadata Wrangling
# DESeq expects the metadata matrix to have sample IDs in the rownames
metadata$Run <- as.character(metadata$Run)
rownames(metadata) = metadata$Run
head(metadata)
# Only keep columns of interest
metadata = metadata[, c("Sample.Characteristic.stimulus."), drop=FALSE] #Drop are used to make data frame with one column
# Look at metadata to see how the variables change with respect to each other
metadata
# Rename column
colnames(metadata) = "treatment"
# Turn stimulus/treatment into a factor
metadata$treatment = factor(metadata$treatment, levels =c("BMP2", "PBS"))
dds <- DESeqDataSetFromMatrix(countData=counts, colData=metadata, design=~treatment)
# Ignore genes with low counts
dds <- dds[rowSums(counts(dds)) > 10, ]
# Run DESeq
dds <- DESeq(dds)
# Compare expression
res <- results(dds, contrast=c("treatment", "BMP2", "PBS"), alpha=0.05)
na.omit(res)
# Convert DESeq2 result into dataframe
res_df <- as.data.frame(res)
#Top 10 Gene
#Filter pvalue and Log2FoldChange
res_df_filter <- res_df[(res_df$padj < 0.05) & (abs(res_df$log2FoldChange) > 2.5),]
res_df_filter <- na.omit(res_df)
#Ordered padj
top10_gene_ordered <- res_df_filter[order(res_df_filter$padj, res_df_filter$log2FoldChange), ]
# Top10 gene base on padj and fold2 change
top10_gene <- head(top10_gene_ordered, 10)
top10_gene_df <- as.data.frame(top10_gene)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_gene_df), keytype = "ENSEMBL", column = "SYMBOL")
#Add gene ID column
res_df_filter$Gene.ID <- rownames(res_df_filter)
top10_gene_df$Gene.ID <- rownames(top10_gene_df)
#Annotate gene ID
res_df_filter$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(res_df_filter), keytype = "ENSEMBL", column = "SYMBOL")
top10_gene_df$Gene.Names <- mapIds(org.Hs.eg.db,keys = rownames(top10_gene_df), keytype = "ENSEMBL", column = "SYMBOL")
View(top10_gene_df)
View(res_df_filter)
View(top10_gene_df)
if (!require("BiocManager", quietly = TRUE))  {
install.packages("BiocManager")
}
# 2. Install paket Bioconductor (GEOquery & limma)
#GEOquery: mengambil data dari database GEO
#limma: analisis statistik ekspresi gen
BiocManager::install(c("GEOquery", "limma"), ask = FALSE, update = FALSE)
#Install annotation package sesuai platform
#GPL96 = Affymetrix Human Genome U133A
BiocManager::install("hgu133a.db", ask = FALSE, update = FALSE)
install.packages(c("pheatmap", "ggplot2", "dplyr"))
if (!requireNamespace("umap", quietly = TRUE)) {
install.packages("umap")
}
library(GEOquery)
library(limma)
library(pheatmap)
library(ggplot2)
library(dplyr)
library(hgu133a.db)
library(AnnotationDbi)
library(umap)
gset <- getGEO("GSE10072", GSEMatrix = TRUE, AnnotGPL = TRUE)[[1]]
# exprs(): mengambil matriks ekspresi gen
# Baris  = probe/gen
# Kolom  = sampel
ex <- exprs(gset)
View(gset)
View(ex)
#quantile(): menghitung nilai kuantil (persentil)
#as.numeric(): mengubah hasil quantile (yang berupa named vector)
#menjadi vektor numerik biasa agar mudah dibandingkan
qx <- as.numeric(quantile(ex, c(0, 0.25, 0.5, 0.75, 0.99, 1), na.rm = TRUE))
class(qx)
qx[5]
qx
if (LogTransform) {
# Nilai <= 0 tidak boleh di-log, maka diubah menjadi NA
ex[ex <= 0] <- NA
ex <- log2(ex)
}
#LogTransform adalah variabel logika (TRUE / FALSE)
#Operator logika:
#>  : lebih besar dari
#|  : OR (atau)
#&& : AND (dan)
LogTransform <- (qx[5] > 100) | | (qx[6] - qx[1] > 50 && qx[2] > 0)
#LogTransform adalah variabel logika (TRUE / FALSE)
#Operator logika:
#>  : lebih besar dari
#|  : OR (atau)
#&& : AND (dan)
LogTransform <- (qx[5] > 100) |  (qx[6] - qx[1] > 50 && qx[2] > 0)
if (LogTransform) {
# Nilai <= 0 tidak boleh di-log, maka diubah menjadi NA
ex[ex <= 0] <- NA
ex <- log2(ex)
}
roup_info <- pData(gset)[["source_name_ch1"]]
group_info <- pData(gset)[["source_name_ch1"]]
group_info
groups <- make.names(group_info)
#make.names(): mengubah teks menjadi format valid untuk R
groups <- make.names(group_info)
class(gset)
View(gset)
#factor():
#Mengubah data kategorik menjadi faktor
#Faktor sangat penting untuk analisis statistik di R
gset$group <- factor(groups)
class(gset)
classs*gset@group
class(gset$group)
nama_grup <- levels(gset$group)
print(nama_grup)
library(GEOquery)
library(limma)
library(pheatmap)
library(ggplot2)
library(dplyr)
library(hgu133a.db)
library(AnnotationDbi)
library(umap
)
set <- getGEO("GSE10072", GSEMatrix = TRUE, AnnotGPL = TRUE)[[1]]
gset <- getGEO("GSE10072", GSEMatrix = TRUE, AnnotGPL = TRUE)[[1]]
ex <- exprs(gset)
#quantile(): menghitung nilai kuantil (persentil)
#as.numeric(): mengubah hasil quantile (yang berupa named vector)
#menjadi vektor numerik biasa agar mudah dibandingkan
qx <- as.numeric(quantile(ex, c(0, 0.25, 0.5, 0.75, 0.99, 1), na.rm = TRUE))
LogTransform <- (qx[5] > 100) |  (qx[6] - qx[1] > 50 && qx[2] > 0)
LogTransform <- (qx[5] > 100) |  (qx[6] - qx[1] > 50 && qx[2] > 0)
if (LogTransform) {
# Nilai <= 0 tidak boleh di-log, maka diubah menjadi NA
ex[ex <= 0] <- NA
ex <- log2(ex)
}
#pData(): metadata sampel
#source_name_ch1 berisi informasi kondisi biologis sampel
group_info <- pData(gset)[["source_name_ch1"]]
#make.names(): mengubah teks menjadi format valid untuk R
groups <- make.names(group_info)
#factor():
#Mengubah data kategorik menjadi faktor
#Faktor sangat penting untuk analisis statistik di R
gset$group <- factor(groups)
View(gset)
class(gset)
View(gset)
#make.names(): mengubah teks menjadi format valid untuk R
groups <- make.names(group_info)
View(ex)
nama_grup <- levels(gset$group)
print(nama_grup)
#model.matrix():
#Membuat matriks desain untuk model linear
#~0 berarti TANPA intercept (best practice limma)
design <- model.matrix(~0 + gset$group)
View(ex)
View(design)
#colnames(): memberi nama kolom agar mudah dibaca
colnames(design) <- levels(gset$group)
levels(gset$group)
grup_kanker <- nama_grup[1]
grup_normal <- nama_grup[2]
contrast_formula <- paste(grup_kanker, "-", grup_normal)
print(paste("Kontras yang dianalisis:", contrast_formula))
print("Kontras yang dianalisis:", contrast_formula)
print(paste("Kontras yang dianalisis:", contrast_formula))
#lmFit():
#Membangun model linear untuk setiap gen
fit <- lmFit(ex, design)
View(fit)
getwd()
save.iamge(file = "Coding_GSE100072.Rhistor")
save.iamge(file = "Coding_GSE100072".Rhistory)
save.image(file = "Coding_GSE100072".Rhistory)
save.image(file = "Coding_GSE100072.RData")
